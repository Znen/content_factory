# Техническое задание — Content Factory (`generation-factory`)

**Дата:** 2026-07-05 · **Обновлено:** 2026-07-09
**Статус:** Фаза 1 + промпт-слой + Magnific-бэкенд (картинки) + видео-паспорта + видео-бэкенд Dreamina — выполнены; Фазы 2b–3 — планируются.

## 1. Назначение

Отдельный MCP-сервер, дающий агенту Hermes генеративные инструменты для визуального препрода. Ставится рядом с уже подключёнными к Hermes `knowledge-factory` и `magnific`.

## 2. Функциональные требования

### 2.1. MCP-инструменты

| Инструмент | Фаза | Что делает |
|---|---|---|
| `gf_generate_draft(project, prompt, n, negative, seed, raw=False)` | ✅ 1 / ✅ Ф-П3 | N дешёвых черновиков через ComfyUI → `media/generated/<дата>/_drafts/draft-NN.png`. `raw=False` (default): `prompt` трактуется как задача и переписывается промпт-райтером под `comfyui/sdxl-juggernaut`; `raw=True` — текст уходит в модель дословно (старое поведение) |
| `gf_generate_final(project, prompt, refs[], aspect, raw=False)` | ✅ 1 / ✅ Ф-П3 | Финал через Nano Banana (≤4 multi-референса) → `media/generated/<дата>/`. `raw=False`: переписывается под `nano/gemini-image`; `raw=True` — дословно |
| `gf_write_prompt(project, target, task, refs[], aspect, extra)` | ✅ Ф-П3 | Явный вызов промпт-райтера для любой цели из `gf_list_targets` (в т.ч. видео-модели и площадки, которые завод не исполняет — Hermes получает промпт и зовёт их сам) |
| `gf_list_targets()` | ✅ Ф-П3 | Список целей райтера с их паспортами (frontmatter `gf/promptdocs/`) |
| `gf_generate_magnific(project, model, prompt, refs[], aspect, raw=False)` | ✅ M | Генерация картинки через **Magnific API** (REST, `api.magnific.com`): `model` — обязателен (`mystic`/`seedream-v4-5-edit`/`flux-kontext-pro`), target паспорта `magnific/<model>`; референсы base64 в теле, поллинг задачи, скачивание CDN-URL. `aspect` — enum Magnific. Спека `2026-07-06-magnific-backend-design.md` |
| `gf_save_asset(project, image, kind, name)` | ⬜ 2 | Сохранить выбранное в кирпич `locations/<name>/` или `characters/<name>/` |
| `gf_assemble_shot(project, shot, character, location, prompt, date)` | ⬜ 2 | Сборка кадра (лицо+локация multi-ref) → `generated/<дата>/shot-XX/` + промпт в `prompts/` |
| `gf_contact_sheet(project, selection)` | ⬜ 3 | Контакт-лист ПО ЗАПРОСУ (за дату / последние N / список) |

`project` — абсолютный путь к папке проекта; Hermes передаёт его явно.

### 2.2. Раскладка медиа проекта (storage-спека)

```
<project>/media/
├── references/{characters,locations,look-and-feel}/   ← входящее из брифа
├── locations/<name>/                                  ← созданные кирпичи-локации
├── characters/<name>/                                 ← утверждённые персонажи
├── generated/<YYYY-MM-DD>/
│   ├── _drafts/draft-NN.png
│   └── shot-XX/{iter-*.png, prompts/iter-*.json}
└── storyboards/
```

### 2.3. Промпты / память процесса

- Промпты сохраняются рядом с картинками: `generated/<дата>/shot-XX/prompts/iter-*.json` (промпт + backend + референсы + seed + дата), связь по имени.
- Назначение: (а) отдать клиенту; (б) воспроизвести удачное / избежать неудачного. Hermes перед новым кадром читает прошлые удачные промпты.

### 2.4. Cost-gating

- Каждая генерация логируется в `<project>/media/.gf_cost_log.jsonl` (оценка стоимости).
- Опциональный мягкий кап `GF_IMAGE_CAP_USD`: генерация, превышающая накопленный лимит проекта, **отклоняется** (возврат `{"error": ...}`, ничего не генерируется).
- Нулевая стоимость (локальный ComfyUI) **не блокируется** капом никогда.

### 2.5. Промпт-слой (промпт-райтер)

Причина: Hermes (deepseek) сам пишет слабые промпты для генеративных моделей — не знает синтаксис конкретной площадки, теряет контекст брифа. Отдельный слой поручает написание промпта Claude API, по markdown-«паспорту» модели. Спека: `docs/superpowers/specs/2026-07-06-prompt-layer-design.md`.

- **Инструменты:** `gf_write_prompt` (явный вызов, работает для любой цели, включая `magnific/*` — площадки, которые `gf` не оборачивает своими бэкендами) и `gf_list_targets` (справочник доступных целей); плюс неявный авто-путь `raw=False` внутри `gf_generate_draft`/`gf_generate_final` (draft переписывается под `comfyui/sdxl-juggernaut`, final — под `nano/gemini-image`; `raw=True` — старое поведение без райтера).
- **Цели/паспорта:** markdown-файл с YAML-frontmatter (`gf/promptdocs/<platform>/<model>.md`), обязательные ключи `target/platform/syntax/negative/refs/updated`; тело — структура идеального промпта, типовые фейлы, примеры «задача → плохо → хорошо». Базовые 6 целей: `comfyui/sdxl-juggernaut`, `nano/gemini-image`, `magnific/{mystic,seedream-v4-5-edit,flux-kontext-pro,kling-v2-5-pro}`; плюс `magnific/_catalog.md` — справочник «когда какую модель Magnific брать» (не цель).
- **Видео-паспорты (⬜ в работе, спека `2026-07-09-video-prompt-passports-design.md`):** 4 новые цели `video/{veo-3-1,seedance-2-0,wan-2-5,runway-gen-4}` (t2v + i2v; Veo — с нативным аудио; Seedance — multi-shot). Группируются по модели (папка `video/`, `platform: <семейство>`), т.к. синтаксис промпта определяет сама модель, а не путь исполнения. Путь исполнения для генерации пока не выбран — паспорта работают в явном режиме (`gf_write_prompt` → промпт Hermes'у). Для **Seedance** путь известен и отработан — **Dreamina CLI (JiMeng)**; первоисточник фактуры — `Q:\Lion Films. AI studio\tools\dreamina-cli\DREAMINA_CLI.md` и `…\videogen\backends\dreamina.py`.
- **Контекст вызова:** паспорт цели + `<project>/prompts/style.md` (опционально, отсутствие — не ошибка) + задача/референсы(имена)/аспект/доп.указания из аргументов → один forced tool call к Claude (`emit_prompt`) → `{prompt, negative, params, notes}`. Провенанс логируется в `<project>/prompts/YYYY-MM-DD-<slug>-NN.md`.
- **Конфиг (`GF_WRITER_*`):** `GF_WRITER_MODEL` (default `claude-sonnet-5`), `GF_WRITER_MAX_TOKENS` (default 2000), `GF_WRITER_ENABLED` (default true — выключен: явный вызов возвращает ошибку, авто-путь тихо пропускает райтера), `GF_WRITER_DRAFT_TARGET`/`GF_WRITER_FINAL_TARGET` (цели авто-пути, дефолты — comfyui/nano выше). Ключ — стандартный `ANTHROPIC_API_KEY` (env/`.env`).
- **Обработка ошибок:** явный `gf_write_prompt` — fail-closed (`{"error": ...}` с инструкцией, без traceback); авто-путь — fail-open (генерация продолжается с текстом задачи как есть, в ответе флаг `writer_skipped: "<причина>"`).
- **Статус:** код реализован и покрыт unit-тестами (без сети, инжектируемый клиент); бесключевые CLI-смоуки (`gf list-targets`, чистая fail-closed ошибка `gf write-prompt` без ключа) пройдены. Живой смоук (реальный ответ Claude) и регистрация новых инструментов в Hermes-gateway (рестарт discovery) ждут `ANTHROPIC_API_KEY` в системе.

## 3. Нефункциональные требования

- **Портируемость / local-first:** без внешних облачных зависимостей в ядре; всё локально (ComfyUI на RTX 4070, Nano-сервер на localhost). Секреты — вне репозитория.
- **Платформа:** Windows 11, Python ≥ 3.10 (проверено на 3.10.11).
- **Пакет:** `gf`; CLI `gf`; env-префикс `GF_`; MCP-имя `generation-factory`; порт по умолчанию **8766** (чтобы не биться с kf `:8765`).
- **Транспорты MCP:** stdio (по умолчанию, для локального Hermes-subprocess); HTTP+bearer (`hmac.compare_digest`) когда задан `GF_MCP_TOKEN`; plain streamable-HTTP иначе.
- **Тесты:** каждый модуль покрыт; HTTP-бэкенды тестируются с инъекцией фейковой сессии (без сети). Реальные внешние вызовы — за маркером `live` (`GF_RUN_LIVE=1`).
- **Простота:** без pydantic (обычный `@dataclass` config), без БД-стека (в отличие от kf).

## 4. Бэкенды и их текущий статус

| Backend | Роль | Статус |
|---|---|---|
| **ComfyUI** | дешёвые черновики; кандидат в основной генератор стиллов | ✅ **прогнан против живого ComfyUI** (2026-07-05): реальный draft работает end-to-end |
| **Nano Banana** | финал по промпту/референсам | клиент готов и отвязан; **заблокирован**: Gemini 403 — ключ без доступа к `gemini-3-pro-image-preview`; роль финала перенимает Magnific |
| **Magnific API** | **картинки** (t2i/edit multi-ref: Mystic/Seedream 4.5 Edit/Flux Kontext Pro) — замена заблокированному Nano; upscale и видео — позже | ✅ **бэкенд завода реализован (REST `api.magnific.com`)** — коммит 99d7849, инструмент `gf_generate_magnific`, спека `2026-07-06-magnific-backend-design.md`. Header `x-magnific-api-key` (`GF_MAGNIFIC_API_KEY`); референсы base64 в теле (upload-флоу НЕ нужен); POST-пути per-model; aspect — enum; завод ждёт сам (поллинг, таймаут → task_id). Live mystic → PNG 2048². Осталось: рестарт gateway |
| **Видео** (Veo / Seedance / Wan / Runway / Kling) | завод пока только **пишет промпты** (паспорта), не исполняет генерацию | генерация видео заводом — будущая фаза; для **Seedance** есть отработанный путь **Dreamina CLI (JiMeng)** и рабочий образец оркестрации `videogen/backends/dreamina.py` (staging путей без пробелов, retry+backoff, VIP-очередь, async-poll) |

## 5. Осознанно выброшено (не-цели)

Старая структура `00_brief..05_review`, `.lf_project`, scaffold/lint; реестры-гейты `reference_register.json`/`shot_registry.json`/`return.json`; QA-promotion в `library/wins/`; 9 постоянных агентов; авто-сборка контакт-листа; симлинк `projects_live`/`WORK`.

**Cost-gating перенесён как чистая версия, НЕ дословно** — старые `budget.py`/`pricing.py` завязаны на `00_brief/brief.yaml`, `tools_registry.json`, `_REPO_ROOT`; это та машинерия, что выбрасывается.

## 6. Критерии приёмки

- **Фаза 1 (✅ выполнена):** пакет ставится; `gf serve` поднимает stdio MCP `generation-factory` с `gf_generate_draft` + `gf_generate_final`; unit-суита зелёная (32 passed / 1 skipped); сервер реально стартует и регистрирует оба инструмента.
  - ⚠️ **Открытый пункт DoD:** «реальный финал производит PNG» — не закрыт из-за внешнего блокера Gemini 403 (не дефект кода; клиент верифицирован вплоть до генерации).
- **Фаза 2:** `gf_save_asset` + `gf_assemble_shot` работают; кирпичи переиспользуются; промпты пишутся в sidecar.
- **Фаза 3:** `gf_contact_sheet` по запросу; чтение прошлых промптов.
- **Фаза 4:** зарегистрирован в `config.yaml` Hermes; старый агент архивирован.
- **Фаза M (Magnific-бэкенд) ✅ ВЫПОЛНЕНА:** `gf_generate_magnific` генерит картинку через Magnific REST end-to-end (base64-рефы → POST → поллинг → скачивание CDN в `media/`); стоимость гейтится; ответ несёт `prompt_used`/`prompt_log`/`writer_skipped`/`task_id`/`timed_out`; суита 179 passed; живой смоук mystic пройден (PNG 2048²).
- **Фаза V (видео-паспорта):** `gf_list_targets` видит `video/{veo-3-1,seedance-2-0,wan-2-5,runway-gen-4}`; `pytest tests/test_writer.py` зелёный; паспорта в формате и глубине `kling-v2-5-pro.md` (t2v+i2v, аудио у Veo, multi-shot у Seedance).

## 7. Зависимости и внешнее окружение

- **ComfyUI:** `Q:\ComfyUI_windows_portable`, `http://127.0.0.1:8188`. Модели: juggernautXL, realvisXL, epicrealism_xl, sd_xl_base(+refiner), flux1-schnell-fp8, ltx-video.
- **Nano (Nitro Express):** `R:\nitro_banana`, `http://localhost:3001`, пароль в `~/.lionfilms/nitro_banana.env`.
- **Hermes:** `C:\Users\kir_a\AppData\Local\hermes\config.yaml`, ключ `mcp_servers` (см. память `hermes-integration`).

## Связанные документы

[ИДЕЯ](ИДЕЯ.md) · [АРХИТЕКТУРА](АРХИТЕКТУРА.md) · [ЗАДАЧНИК](ЗАДАЧНИК.md) · план Фазы 1: `superpowers/plans/2026-07-03-content-factory-phase-1.md`.
