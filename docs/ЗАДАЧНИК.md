# Задачник — Content Factory

**Дата:** 2026-07-05 · **Обновлено:** 2026-09-11
**Легенда:** ✅ сделано · ⬜ ждёт · ⛔ заблокировано · ❓ открытое решение · 🚧 в работе

**Статус коротко (2026-09-10):** завод генерит картинки (ComfyUI-черновики + Magnific: mystic/seedream/flux/**nano-banana-pro**) и видео (Dreamina/Seedance + Magnific veo/kling как резерв, авто-роутинг). Добавлена thin-интеграция **fal.ai**: async queue submit/poll/result, authenticated workflow listing, MCP tools `gf_fal_run`/`gf_fal_list_workflows`, CLI `fal-run`/`fal-list-workflows`, локальные `@R:/...`/`@C:/...` файлы кодируются в data URI, выходные media-URL скачиваются в `media/generated/<date>/` и `video/` с сохранением URL при сбое скачивания. `FAL_KEY` не настроен, поэтому live paid calls не выполнялись; бесключевой путь даёт структурированную ошибку. Суита **252 passed / 1 skipped**.

---

## Фаза 1 — Скелет + минимальная генерация ✅ ВЫПОЛНЕНА

12 коммитов (`a6b03c4..92976da`), unit-суита **32 passed / 1 skipped**, дерево чистое. Все 9 задач прошли TDD + двухступенчатое ревью, финальный whole-branch review (opus): «Ready to merge: Yes».

- ✅ Скелет (pyproject, config, git), pricing, budget, media, `backends/nano` (ported+отвязан), `backends/comfyui` (с нуля), core+mcp_server (3 транспорта), CLI, README + live-гейт.
- ✅ Фиксы по ходу: `budget.spent` не падает на битом логе; Nano не течёт `AuthExpired` при двойном 401; нулевая стоимость не блокируется капом; `media.generated_dir()` вместо хрупкого `.parent`; тест bearer-middleware; провязка `core.py`.
- ✅ Реальный smoke: сервер поднимается как `generation-factory` на `127.0.0.1:8766`, оба инструмента зарегистрированы.

### ⛔→✅ Пункт DoD Фазы 1 — обойдён (2026-07-09)
- ⛔ **Родной Nano-путь всё ещё заблокирован** — Gemini вернул HTTP 403: у API-ключа нет доступа к `gemini-3-pro-image-preview` (Nitro-сервер хардкодит эту модель). Клиент `gf` верифицирован вплоть до генерации. Также Nitro-сервер разово падал (перезапущен).
- ✅ **Обход завода:** финалы теперь производит **Magnific `nano-banana-pro`** (Google Nano Banana, verified live 2026-07-09, коммит `534c623`) — прямой аналог заблокированной Nano/Gemini-модели, поверх заводского REST-бэкенда Magnific. Реальные PNG получаются. Fix формата рефов — `bab9f41` (см. Фазу M).
- ✅ **Реальный ComfyUI draft-smoke ПРОЙДЕН** (2026-07-05) — ComfyUI 0.21.1 на RTX 4070; `gf_generate_draft` (n=2, juggernautXL, «scandinavian kitchen») выдал два валидных 1024×1024 RGB PNG (~1.4 МБ), cost-лог записан, картинки соответствуют промпту. Путь черновиков подтверждён end-to-end, не на моках.

---

## ❓ Открытые решения (нужен выбор пользователя)

1. ✅ **Стратегия генерации стиллов при заблокированном Nano — РЕШЕНО (2026-07-09):** финалы перенимает **Magnific как бэкенд завода** (t2i/edit multi-ref). Два рабочих пути: `seedream-v4-5-edit` для «лицо+локация» и **`nano-banana-pro`** (Google Nano Banana) — прямой аналог заблокированной Nano/Gemini-модели (verified live, коммит `534c623`). Прежний тезис «Magnific не подходит для стиллов, это только видео+TTS» **устарел** — исследование промпт-слоя (2026-07-06) подтвердило t2i/upscale у площадки, а брейншторм (2026-07-09) решил обернуть её заводом по REST. Спека `2026-07-06-magnific-backend-design.md`. Остаётся жив резервный путь (A) ComfyUI multi-ref (IPAdapter+ControlNet); (B)/(C) починка родного Nano — не в приоритете.
2. ❓ **`gf/core.py`** — оставить провязанным (как сейчас, plan-consistent) или упростить/удалить (ревьюер склонялся к YAGNI-drop). Решить при первом расширении, когда появятся stateful-handles.

3. ✅ Проверить реальные инструменты Magnific — закрыто исследованием промпт-слоя (Ф-П1, 2026-07-06): вместо live `tools/list` подтверждено по документации Freepik-платформы (`gf/promptdocs/magnific/_catalog.md`) — у Hermes-обёртки magnific уже есть **t2i** (`images_generate` с multi-референсами: Mystic/Seedream 4.5 Edit/Flux Kontext Pro и др.) и **upscale** (`images_upscale`, 2x–16x, Upscaler/Upscaler Precision). Роль «только видео + TTS» из ТЗ §4 устарела и там же исправлена. **Обновление 2026-07-09:** решено, что `gf` **будет** оборачивать Magnific своим REST-бэкендом (Фаза M ниже) вместо вызова Hermes'ом напрямую — ради единого бюджета/провенанса/хранилища.

---

## Фаза 2 — Building-blocks ⬜

- ✅ **Ф2a (курация) РЕАЛИЗОВАНА** — наборы вариантов + подвижный winner: `gf_add_variant`, `gf_set_winner`, `gf_list_sets`, `gf_materialize_winners`, `gf_discard`, `gf_adopt_set` (спека `superpowers/specs/2026-07-05-media-curation-routing-design.md`; `gf_save_asset` заменён этой парой инструментов).
- ⬜ Ф2b: `gf_contact_sheet` (PIL-монтаж winner'ов; добавит Pillow).
- ⬜ `gf_assemble_shot(project, shot, ...)` — генеративная сборка кадра из winner'ов кирпичей (lineart+cast+look+brand); частично упирается в Nano/Gemini-блокер.
- ⬜ Датированная структура `shot-XX/` + sidecar-промпты (промпт+backend+референсы+seed+дата).
- ⬜ Уточнить: как режем кадры раскадровки (вероятно по команде «кадр N», не авто-нарезка).
- ⬜ Если выбран путь (A): реализовать multi-ref сборку на ComfyUI (IPAdapter лицо + ControlNet композиция) как первичный бэкенд `gf_assemble_shot`.

## Фаза 3 — Контакт-листы + память процесса ⬜

- ⬜ `gf_contact_sheet(project, selection)` — сборка по запросу (за дату / последние N / список). НЕ авто.
- ⬜ Память процесса: чтение прошлых удачных `prompts/` перед новым кадром; перенос удачных подходов.

## Фаза 4 — Подключение к Hermes + архивация (подключение ✅ 2026-07-05)

- ✅ `generation_factory` зарегистрирован в `config.yaml` Hermes (stdio: WindowsApps python `-m gf.cli serve`, timeout 600). Лог: `registered 12 tool(s)` (8 gf_* + 4 служебных), итого 172 инструмента с 5 серверов. Правило «no raw mv/rm над media/, только gf_*» добавлено в SOUL.md.
- ✅ Попутно починена регрессия гейтвея: откат на uv-pythonw (05.07) молча отключил ВСЕ MCP (pywintypes); фикс — mcp+pywin32 установлены в uv-Python (--break-system-packages). ⚠️ При обновлении uv-python-билда — переустановить.
- ✅ `if __name__=="__main__": main()` добавлен в `cli.py` (коммит 60ffb9f) — `python -m gf.cli serve` работает.
- ⬜ Архивировать старый агент `Q:\Lion Films. AI studio` (НЕ удалять — там 322 мастера Delicados, 22 итерации AIPriest). Забрать только генеративный код уже забран.
- ⬜ Зависит от storage-Фазы 2 (упразднение `WORK`) — желательно, не блокирующе.

---

## Новые направления (брейншторм 2026-07-09)

### Фаза M — Magnific как бэкенд картинок ✅ РЕАЛИЗОВАНА (2026-07-09, коммиты 99d7849 → bab9f41)
Завод сам ходит к **Magnific API** (`api.magnific.com`) по REST и генерит картинки (замена заблокированному Nano). Live-смоук mystic → PNG 2048². Спека `specs/2026-07-06-magnific-backend-design.md` (обновлена под факты — блок «Реальность»).
- ✅ `gf/backends/magnific.py` (HTTP-клиент по образцу `nano.py`, инжектируемый `session`, `MagnificError`): per-model payload (base64-рефы в теле) → POST → поллинг → скачивание CDN-URL.
- ✅ `gf_generate_magnific` в `mcp_server.py` (клон `_generate_final_impl`, `model` обязателен) + CLI-зеркало.
- ✅ `config.py` (`GF_MAGNIFIC_API_KEY`/`_BASE_URL`/`_TIMEOUT`/`_POLL_INTERVAL`), `pricing.estimate_magnific`.
- ✅ **4 картиночные модели:** `mystic` (t2i), `seedream-v4-5-edit` (multi-ref «лицо+локация», 1-5), `flux-kontext-pro` (single input_image), **`nano-banana-pro`** (Google Nano Banana; 0-4 рефа, t2i или multi-ref; коммит `534c623`, verified live) — заводской обход Gemini-403.
- ✅ **Robustness (`9026427`):** абсолютный `project` (fail-closed) + устойчивое CDN-скачивание с backoff-ретраем (оплаченный результат не теряется → `image_urls`+`download_failed`).
- ✅ **Fix формата рефов (`bab9f41`, 2026-07-12):** `reference_images` теперь per-model — `nano-banana-pro` шлёт объекты `{image, mime_type}` (голые строки давали 400 «valid dictionary», рефы игнорировались → модель считалась чистым t2i); seedream/flux — голые base64-строки как раньше.
- **Факт vs дизайн:** поверхность = **Magnific, НЕ Freepik** (host `api.magnific.com`, header `x-magnific-api-key`, ключ `GF_MAGNIFIC_API_KEY`); upload-флоу НЕ нужен (референсы base64 в теле); POST-пути per-model (mystic→`/v1/ai/mystic`, остальные→`/v1/ai/text-to-image/<model>`); aspect — enum для mystic/seedream/flux, обычный формат («1:1»/«9:16») для nano-banana-pro. Ключ в `.env`, ПОДОШЁЛ (~51.6K кредитов).
- ⬜ ОСТАЛОСЬ: рестарт gateway — чтобы Гермес увидел `gf_generate_magnific` и все 4 модели.
- Решения брейншторма (в силе): отдельный инструмент; плоская таблица цен; завод ждёт сам (поллинг, таймаут → `task_id`+`timed_out`). Video/upscale — теперь частично реализованы (см. Фазу M-V ниже).

### Фаза M-V — Magnific как резервный видео-путь ✅ РЕАЛИЗОВАНА (2026-07-10, коммит 64fe399)
Magnific стал **вторым видео-бэкендом** завода (резерв Dreamina) с авто-роутингом по модели.
- ✅ `magnific.generate_video` + `_build_video_payload`: t2v `veo-3-1` (без стартового кадра, нативное аудио) и i2v-семейство `kling-*` (поле кадра `image`; локальный winner → base64). duration/aspect enums различаются по модели.
- ✅ **Авто-роутинг** в `_generate_video_impl`: явная Magnific-модель (kling-*/veo-*) без `backend="dreamina"` → Magnific; иначе Dreamina.
- ✅ **Авто-фолбэк:** при `ExceedConcurrencyLimit`/ret=1310 у Dreamina вызов уходит на Magnific (i2v→kling, t2v→veo) с флагом `fallback="magnific"`.

### Фаза FAL — fal.ai thin backend ✅ РЕАЛИЗОВАНА (2026-09-10, без live paid calls)
Добавлен производственный тонкий бэкенд fal.ai по подтверждённым контрактам, без попытки угадывать цены.
- ✅ `gf/backends/fal.py`: `Authorization: Key <FAL_KEY>`, `POST {GF_FAL_QUEUE_URL}/{endpoint_id}`, robust poll `status_url`, fetch `response_url`, единый публичный `FalError`, защита endpoint ID от URL/path traversal injection.
- ✅ Workflow endpoint IDs `workflows/...` используют тот же invocation contract; список приватных workflow берётся через `GET {GF_FAL_API_URL}/v1/workflows` с `limit/cursor/search/used_endpoint_ids`.
- ✅ Входы: public URL/data URI остаются как есть; явные локальные маркеры `@C:/...` и `@R:/...` рекурсивно кодируются как MIME-correct data URI.
- ✅ **Storage upload (2026-09-10):** маркер внутри `video_urls`/`image_urls`/`audio_urls` не кодируется в base64, а **загружается в fal storage** (`POST https://rest.fal.ai/storage/upload/initiate` → presigned `PUT` без Authorization) и подменяется на `file_url` — эти поля принимают только настоящий HTTP-URL (Seedance `reference-to-video` отклонял data URI). Вне url-полей (напр. в `prompt`) поведение не изменилось. Аплоад идёт с ПОЛНЫМ таймаутом, без 30-секундного клампа `_request_timeout`. `upload_file` / `resolve_upload_markers` + 9 тестов. ⚠️ **Хост и путь выверены вживую (2026):** `rest.fal.ai` (не `fal.ai`) и БЕЗ префикса `/api` — `/api/storage/upload/initiate` отдаёт 404, `/storage/upload/initiate` отдаёт 401 без ключа.
- ✅ Выходы: common keys `images/image/video/audio/files/url` обходятся рекурсивно; скачиваемые media URL сохраняются в `media/generated/<date>/`, video/audio — в `media/generated/<date>/video/`; при сбое скачивания URL возвращается наверх с warning, чтобы оплаченный результат не потерялся.
- ✅ MCP: `gf_fal_run(project, endpoint, input, wait_seconds=None)` требует абсолютный `project`; `gf_fal_list_workflows(...)` проект не требует. Ответы содержат `backend="fal"`, endpoint/status/request_id, `raw_output`, локальные media paths и `media_urls`. `cost_usd=None`, потому что fal pricing endpoint-specific.
- ✅ CLI: `gf fal-run PROJECT ENDPOINT INPUT_JSON` и `gf fal-list-workflows`; invalid JSON возвращается структурированным JSON, не traceback.
- ✅ Проверка: `python -m pytest` → **261 passed / 1 skipped** (после storage-upload).

Короткая проверенная карта fal.ai:
- **Model endpoints:** `fal-ai/nano-banana-pro`; `bytedance/seedance-2.0/text-to-video`, `bytedance/seedance-2.0/image-to-video`, `bytedance/seedance-2.0/reference-to-video` и fast variants; `fal-ai/kling-video/v3/standard/image-to-video`; `fal-ai/wan-25-preview/image-to-video`.
- **Public workflow templates:** <https://fal.ai/workflows/templates> — витрина шаблонов, не список приватных workflow пользователя.
- **Authenticated user workflows:** `gf_fal_list_workflows` / `gf fal-list-workflows`; запуск по endpoint ID вида `workflows/...`.

### Фаза REPLICATE — Replicate thin backend ✅ РЕАЛИЗОВАНА (2026-09-11, без live paid calls)
6-й бэкенд — generic-обёртка над Replicate predictions API, близнец fal.
- ✅ `gf/backends/replicate.py`: `Authorization: Bearer <REPLICATE_API_TOKEN>` (OpenAPI Replicate — bearer; легаси `Token` не используем), единый `ReplicateError`, токен вырезается из любого текста ошибки (включая `error`/`detail` от API).
- ✅ Submit по формату `model`: `<64-hex>` и `owner/name:<64-hex>` → `POST /v1/predictions {version, input}`; `owner/name` → `POST /v1/models/{owner}/{name}/predictions {input}`. Всё прочее (URL, `..`, `//`, лишние сегменты, query) отвергается до сети.
- ✅ Poll `urls.get` (строго same-origin с API — туда уходит токен; фолбэк `/v1/predictions/{id}`) до `succeeded|failed|canceled`; таймаут → `pending` + `id`/`poll_url`; `failed`/`canceled` → ошибка с текстом `error` модели.
- ✅ Files API: любой `@C:/...`-маркер на любой глубине input → `POST /v1/files` (multipart `content` + обязательный `filename` + `type`; ответ 201) → `urls.get` подставляется в input. Аплоад с полным таймаутом (без 30-сек клампа).
- ✅ Выходы: любая http(s)-строка с media-расширением в `output` (голый URL / список / dict) скачивается в `media/generated/<date>/`, video/audio — в `video/`; сбой скачивания → URL в `media_urls` + warning.
- ✅ Общий модуль `gf/backends/_media.py` — скачивание media вынесено из fal (fal-тесты зелёные без правок). Попутно исправлено: у media-объекта без `content_type` расширение из `file_name` раньше игнорировалось.
- ✅ MCP `gf_replicate_run(project, model, input, wait_seconds=None)` (17-й gf_*), CLI `replicate-run`, config `REPLICATE_API_TOKEN` + `GF_REPLICATE_*`. `cost_usd=None` — цена Replicate зависит от модели/железа.
- ⬜ `REPLICATE_API_TOKEN` в `.env` нет → живой смоук не делался; рестарт gateway, чтобы Гермес увидел инструмент.

### Фаза V — видео-паспорты промпт-слоя 🚧
Промпт-слой учится писать промпты под видео-модели. Спека `superpowers/specs/2026-07-09-video-prompt-passports-design.md`; **отдана на исполнение** (первый промпт запущен).
- 🚧 4 паспорта `video/{veo-3-1,seedance-2-0,wan-2-5,runway-gen-4}` (t2v+i2v; Veo — аудио; Seedance — multi-shot), формат/глубина как `kling-v2-5-pro.md`.
- ✅ Дизайн-решение: группировка по модели (папка `video/`), явный режим, путь исполнения пока не выбран.
- ✅ Для **Seedance** первоисточник найден — старый видео-завод: `Q:\Lion Films. AI studio\tools\dreamina-cli\DREAMINA_CLI.md` (команды/лимиты/грабли Dreamina CLI JiMeng) + `…\videogen\backends\dreamina.py` (образец оркестрации).
- Проверка: `gf_list_targets` видит 4 цели, `pytest tests/test_writer.py` зелёный, Python не тронут.

### Фаза VG — видео-генерация заводом (Seedance) 🚧
**Спроектирована 2026-07-09** — спека `superpowers/specs/2026-07-09-video-backend-dreamina-design.md`; готова к исполнению терминальным Claude Code.
- ⬜ `gf/backends/dreamina.py` — subprocess-обёртка над `dreamina` CLI по образцу `videogen/backends/dreamina.py` (staging путей без пробелов + retry/backoff + VIP). 4 режима: i2v/t2v/frames/multimodal.
- ⬜ **Гибрид-ожидание:** `--poll` ~180с; не успел → `submit_id` в реестр. Реестр `<project>/media/.gf_video_jobs.jsonl` — завод сам помнит номерки.
- ⬜ Три инструмента: `gf_generate_video` (запуск), `gf_list_video_jobs` (реестр+статусы), `gf_fetch_video` (дозабрать по submit_id). Промпт через `video/seedance-2-0` (авто-путь).
- ⬜ Обязательно: resolution-matching (сжатие input), pre-TNS-обработка, 1080p Seedance только через `multimodal2video --model_version=seedance2.0_vip`.
- ❓ Курс кредита JiMeng → USD не зафиксирован (для бюджет-учёта) — уточнить у Кира.
- Дальше (не v1): Veo/Wan/Runway — другие платформы, отдельные бэкенды.

## Промпт-слой (Ф-П1–Ф-П3) — открытые хвосты

Код реализован и покрыт unit-суитой (спека `superpowers/specs/2026-07-06-prompt-layer-design.md`, план `superpowers/plans/2026-07-06-prompt-layer.md`). Не блокируют текущий v1:

- ⬜ **Живой смоук `gf write-prompt` (реальный ответ Claude) + рестарт Hermes-gateway** (подхват `gf_write_prompt`/`gf_list_targets` при discovery) — оба ждут `ANTHROPIC_API_KEY` в системе. Бесключевые проверки уже пройдены: `gf list-targets` отдаёт все 6 целей, `gf write-prompt` без ключа возвращает чистую fail-closed ошибку в JSON (не traceback).
- ❓ **Vision-режим райтера** — сейчас `refs` доходят до Claude только именами файлов (сами картинки не прикладываются); передача референсов как изображений (для Nano/Magnific multi-ref) — интерфейс уже это допускает, добавится малым шагом, если качество текстового промпта без визуального контекста упрётся.
- ❓ **`<project>/prompts/style.md` — кто заполняет первым**: Кир вручную до начала проекта или Hermes сам по брифу. Решится на первом реальном проекте (принятие Рябы).
- ⬜ **Follow-up:** budget-gate ветка `_generate_draft_impl`/`_generate_final_impl` (`mcp_server.py`) не пробрасывает уже посчитанные `prompt_used`/`prompt_log`/`writer_skipped` — при отклонении генерации капом ранний `return {"error": ...}` их не включает (только `error/images/backend/cost_usd/spent_usd`). Провенанс переписанного промпта теряется именно на отклонённом вызове; не покрыто тестом.

---

## Технический долг (отложено, non-blocking)

Из леджера `.superpowers/sdd/progress.md` и финального ревью:

- ⬜ `comfyui._poll`: обернуть `ConnectionError` (как в `_submit`) + floor-sleep при `poll_interval=0`.
- ⬜ `test_config::test_defaults` не герметичен vs реальный `.env` в CWD (monkeypatch `load_dotenv`/`chdir`).
- ⬜ Выделенный тест aspect-валидации в `nano.generate`.
- ⬜ `config`: дружелюбная ошибка на битые `GF_MCP_PORT`/`GF_IMAGE_CAP_USD` (сейчас голый `ValueError`).
- ⬜ Опционально объявить `uvicorn`/`starlette` в `pyproject` (сейчас транзитивно через `mcp`).
- ⬜ `refs` аннотация уже `list | None`; README выровнен на `refs=None`.

---

## Ближайший шаг (рекомендация)

Разблокировать **реальную** генерацию, чтобы «первый проект» стал настоящим:

1. ✅ Фазы V (видео-паспорта), M (Magnific-картинки, +nano-banana-pro), M-V (Magnific-видео-резерв) и VG (видео-бэкенд Dreamina) — **реализованы** (коммиты 43727ff/99d7849/534c623/64fe399/f0d2423, суита **220 passed / 1 skipped**).
2. ⬜ **Рестарт Hermes-gateway** — чтобы Гермес увидел `gf_generate_magnific` и все 4 модели. После — завод умеет черновики+финалы-картинки+видео.
3. ⬜ Живые смоуки в реальном проекте: Magnific-финал «лицо+локация» (seedream-v4-5-edit) и nano-banana-pro с рефами (проверить фикс `bab9f41` вживую), видео из winner-кадра (Dreamina i2v); ComfyUI-draft для полноты.
4. ⬜ Актуализировать спеку Magnific под факты — ✅ сделано (блок «Реальность»). ❓ курс кредита JiMeng→USD для бюджет-учёта видео.

## Связанные документы

[ИДЕЯ](ИДЕЯ.md) · [ТЗ](ТЗ.md) · [АРХИТЕКТУРА](АРХИТЕКТУРА.md) · план Фазы 1: `superpowers/plans/2026-07-03-content-factory-phase-1.md`.
